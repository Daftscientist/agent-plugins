# Compatibility

Plugins target the Agent Plugins schema declared in their manifests and document their MCP protocol/runtime compatibility independently. Office is tested through the official MCP Python SDK v2 in both modern auto-negotiated and supported legacy client modes; its packaged default transport is stdio.
